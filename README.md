# simplecalculator
A simple calculator using a Chain Of Responsability Pattern OR a RPN (Reverse Polish Notation)

The application can take as argument a file with the operations

Example :

java -jar ./target/simplecalculator-1.0-SNAPSHOT.jar ./src/main/resources/myCalcFile.txt
